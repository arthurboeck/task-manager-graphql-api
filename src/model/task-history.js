export default class TaskHistory {
    constructor(idTask, descricao, usuario, data) {
        this.idTask = idTask;
        this.descricao = descricao;
        this.usuario = usuario;
        this.data = data;
    }
};